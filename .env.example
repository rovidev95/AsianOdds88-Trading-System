# AsianOdds88 Trading System — copy to .env and fill in. Do not commit .env.

# Mode: DRY (no real bets) or LIVE
EXECUTION_MODE=DRY

# Fixed stake per bet (units)
FIXED_STAKE=0.30

# Redis (default localhost:6379)
REDIS_HOST=localhost
REDIS_PORT=6379

# AsianOdds88 credentials are managed by the ingest (Login/Register).
# Do not put passwords here. Optional: AO_BASE_URL if using proxy/cluster.
